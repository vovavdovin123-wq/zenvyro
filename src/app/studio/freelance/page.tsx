import { StudioFreelance } from "@/studio/StudioFreelance";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Фриланс — Студия",
};

export default function StudioFreelancePage() {
  return <StudioFreelance />;
}
